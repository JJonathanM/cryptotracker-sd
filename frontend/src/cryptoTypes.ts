// Archivo adicional para definir tipos TypeScript
export interface CryptoPrice {
  id: string;
  name: string;
  symbol: string;
  price: number;
  image: string;
}

export interface CryptoOption {
  id: string;
  name: string;
}